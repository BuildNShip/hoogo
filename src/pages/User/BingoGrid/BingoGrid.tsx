import React from "react";
import styles from "./BingoGrid.module.css";
type Props = {};

const BingoGrid = (props: Props) => {
  return <div className={styles.mainContainer}>BingoGrid</div>;
};

export default BingoGrid;
